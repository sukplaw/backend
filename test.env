app.post("/api/login", (req, res) => {
  const { usernameOrEmail, password } = req.body;

  if (!usernameOrEmail || !password) {
    return res.status(400).json({ error: "กรุณาป้อนชื่อผู้ใช้และรหัสผ่าน" });
  }

  const sql = `
    SELECT * FROM login
    WHERE (username = ? OR email = ?)
  `;

  pool.query(sql, [usernameOrEmail, usernameOrEmail], async (err, results) => {
    if (err) {
      console.error("Error fetching user:", err);
      return res.status(500).send("Error fetching user");
    }

    if (results.length === 0) {
      return res
        .status(401)
        .json({ error: "ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง" });
    }

    const user = results[0];

    try {
      const isMatch = await bcrypt.compare(password, user.password);

      if (!isMatch) {
        return res
          .status(401)
          .json({ error: "ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง" });
      }

      const token = jwt.sign(
        { id: user.id, username: user.username },
        JWT_SECRET,
        { expiresIn: "1h" }
      );

      // Add a success message to the terminal
      console.log(
        `User '${user.username}' (ID: ${user.id}) successfully logged in.`
      );

      res.json({
        tokenKey: token,
        user: {
          id: user.id,
          username: user.username,
          permission: user.permission,
        },
      });
    } catch (error) {
      console.error("Error during login process:", error);
      res.status(500).send("Internal server error");
    }
  });
});

/////////////แก้ไขโค้ดให้ตรวจสอบแบบปกติ
// ในไฟล์ app.js
// ...
// app.post("/api/login", (req, res) => {
//   const { usernameOrEmail, password } = req.body;

//   // ตรวจสอบว่ามีการส่งข้อมูลครบถ้วนหรือไม่
//   if (!usernameOrEmail || !password) {
//     return res.status(400).json({ error: "กรุณาป้อนชื่อผู้ใช้และรหัสผ่าน" });
//   }

//   // ดึงข้อมูลผู้ใช้จากฐานข้อมูล
//   const sql = `
//     SELECT * FROM login
//     WHERE (username = ? OR email = ?)
//   `;

//   // แก้ไข db เป็น pool
//   pool.query(sql, [usernameOrEmail, usernameOrEmail], (err, results) => {
//     if (err) {
//       console.error("Error fetching user:", err);
//       return res.status(500).send("Error fetching user");
//     }

//     if (results.length === 0) {
//       return res.status(401).json({ error: "ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง" });
//     }

//     const user = results[0];

//     // **แก้ไขส่วนนี้**
//     // ตรวจสอบรหัสผ่านโดยตรงจากฐานข้อมูล (วิธีที่ไม่ปลอดภัย)
//     if (password !== user.password) {
//       return res.status(401).json({ error: "ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง" });
//     }
//     // ... โค้ดส่วนที่เหลือจะเหมือนเดิม
//   });
// });
// const express = require("express");
// const app = express();
// const cors = require("cors");
// const bodyParser = require("body-parser");
// const mysql = require("mysql2");

// app.use(
//   cors(),
//   bodyParser.json({
//     limit: "50mb",
//   }),
//   bodyParser.urlencoded({
//     extended: true,
//   }),
//   require("./router.js")
// );

// app.get("/", function (req, res) {
//   res.send(["api work!"]);
// });

// let port = app.listen(3006, function () {
//   console.log(`server running on port ${port.address().port}`);
// });
[
  {
    "username": "service1",
    "email": "tawan@example.com",
    "password": "Taw@n2023",
    "role": "service"
  },
  {
    "username": "service2",
    "email": "nattapong@example.com",
    "password": "N@ttP456",
    "role": "service"
  },
  {
    "username": "service3",
    "email": "boonchai@example.com",
    "password": "Bo0n#789",
    "role": "service"
  },
  {
    "username": "service4",
    "email": "korn@example.com",
    "password": "Korn!999",
    "role": "service"
  },
  {
    "username": "service5",
    "email": "phuwadet@example.com",
    "password": "Phuw@123",
    "role": "service"
  },
  {
    "username": "service6",
    "email": "jakrapong@example.com",
    "password": "Jak#P202",
    "role": "service"
  },
  {
    "username": "service7",
    "email": "anusorn@example.com",
    "password": "Anu$2024",
    "role": "service"
  },
  {
    "username": "service8",
    "email": "krit@example.com",
    "password": "Krit!X88",
    "role": "service"
  },
  {
    "username": "service9",
    "email": "veerachai@example.com",
    "password": "Veer@789",
    "role": "service"
  },
  {
    "username": "service10",
    "email": "chokchai@example.com",
    "password": "Ch0k@456",
    "role": "service"
  }
]
[
  {
    "username": "service1",
    "email": "arthit@example.com",
    "password": "Arth!t2023",
    "role": "service"
  },
  {
    "username": "service2",
    "email": "supansa@example.com",
    "password": "Sup@1234",
    "role": "service"
  },
  {
    "username": "service3",
    "email": "kittipong@example.com",
    "password": "Kit#7890",
    "role": "service"
  },
  {
    "username": "service4",
    "email": "nalinee@example.com",
    "password": "N@lin2022",
    "role": "service"
  },
  {
    "username": "service5",
    "email": "wichai@example.com",
    "password": "Wich@i01",
    "role": "service"
  },
  {
    "username": "service6",
    "email": "pimchanok@example.com",
    "password": "P!mchN99",
    "role": "service"
  },
  {
    "username": "service7",
    "email": "anurak@example.com",
    "password": "Anu#2023",
    "role": "service"
  },
  {
    "username": "service8",
    "email": "chonticha@example.com",
    "password": "Ch0n$456",
    "role": "service"
  },
  {
    "username": "service9",
    "email": "surasak@example.com",
    "password": "Sura@K88",
    "role": "service"
  },
  {
    "username": "service10",
    "email": "kanokwan@example.com",
    "password": "K@nok2024",
    "role": "service"
  }
]
